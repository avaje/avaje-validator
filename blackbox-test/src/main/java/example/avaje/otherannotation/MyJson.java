package example.avaje.otherannotation;

/**
 * An annotation we put on the class level that should be recognised as
 * NOT a constraint (no class level constraint for this annotation)
 */
public @interface MyJson {
}
